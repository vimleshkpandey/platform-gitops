sealed-secrets:
  fullnameOverride: sealed-secrets-controller
  namespace: kube-system

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  metrics:
    serviceMonitor:
      enabled: false
